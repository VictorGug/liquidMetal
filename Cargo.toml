[package]
name = "liquid-metal"
version = "0.1.0"
edition = "2024"

[dependencies]
glow = "0.18.0"
sdl2 = { version = "0.38.0", features = ["bundled", "raw-window-handle", "static-link"] }
x11rb = { version = "0.14.0", features = ["shape", "randr"] }
